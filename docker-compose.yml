services:
  postgres:
    image: postgres:16-alpine
    container_name: cloudpilot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cloudpilot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cloudpilot_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-cloudpilot_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cloudpilot} -d ${POSTGRES_DB:-cloudpilot_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cloudpilot-network

  redis:
    image: redis:7-alpine
    container_name: cloudpilot-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cloudpilot-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  cloudpilot-network:
    driver: bridge
